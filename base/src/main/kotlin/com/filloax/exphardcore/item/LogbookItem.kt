package com.filloax.exphardcore.item

// Shared by ExpeditionersLogbookItem and SignedExpeditionersLogbookItem, so
// protections that should apply to both (despawn, etc.) don't need to special-case each type.
interface LogbookItem
